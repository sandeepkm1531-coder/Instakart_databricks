# Databricks notebook exports

These Jupyter notebooks were supplied from the Databricks workspace on September 22, 2026. Cell source and order are preserved; saved outputs, execution counts, and workspace metadata are omitted.

- `02_Silver_Layer.ipynb`: matches the maintained Silver source notebook.
- `03_Gold_Layer.ipynb`: preserves the workspace version, including its exploratory SQL cells. It differs from the maintained Gold source: it uses `_gold_processed_at_utc` instead of `gold_updated_at` and lacks the additional null and cross-mart reconciliation checks in the maintained version.

The `.py` notebooks in the parent directory remain the maintained pipeline. These exports are reference snapshots, not a replacement for that pipeline or evidence of successful execution.
